
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
<%- include('./header.ejs') %> 
<%- include('./left.ejs') %>
<div class="container-fluid">
    <div class="row">
        <div class="col-3"></div>
        <div class="col-md-9">
         <h2 class="text-center">View the Contact imformation </h2>
         <% if(contdata.length>0){ %>
        
            <table class=" table table-bordered table-sm ">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Email</th>
                        <th>Number</th>
                        <th>Message</th>
                        <th>edit</th>
                    </tr>
                </thead>
                <tbody>
                    <% for(var i=0; i<contdata.length; i++){%>
                        <tr>
                            <td><%= contdata[i].yourname %></td>
                            <td><%= contdata[i].email %></td>
                            <td><%= contdata[i].Number %></td>
                            <td><%= contdata[i].Message %></td>
                            <td><a href="/delete_3/<%= contdata[i]._id %>"><button class="btn-primary">delete</button></a></td>
                            
 
                        </tr>

                        <%} %>
                        
                </tbody>
                </table>
                <%} else
                {%>
                    no record found
               <% } %>
        </div>
    </div>
</div>



</body>
</html>